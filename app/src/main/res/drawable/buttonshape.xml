<shape xmlns:android="http://schemas.android.com/apk/res/android">
    <solid android:color="@color/white" /> <!-- Warna latar belakang -->
    <corners android:radius="12.8dp" /> <!-- Jari-jari radius yang diinginkan -->
    <stroke
        android:width="1dp"
        android:color="#66000000" />
    <corners android:radius="8dp" /> <!-- Use the same radius as in the shadow drawable -->
</shape>